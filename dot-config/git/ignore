# direnv files - loaded per-directory environment variables
.envrc
.direnv/
.sisyphus/
.omc/

# Notes vault - synced via Syncthing, not git
.notes
.notes/

# Agent instructions - local context only, not committed
# To track in a specific repo: add `!AGENTS.md` (or `!CLAUDE.md`) to that repo's .gitignore
AGENTS.md
CLAUDE.md
CLAUDE.local.md

# Claude Code local settings - machine-specific, never committed
**/.claude/settings.local.json
.claude/

# GSD planning artifacts - per-project working state, not committed by default
.planning/

# Superpowers working docs - plans/specs, local context only
docs/superpowers/
