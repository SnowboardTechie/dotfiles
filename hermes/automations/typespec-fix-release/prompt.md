Script-only TypeSpec stable-release status reminder for SGG PR #1093. Deliver script stdout verbatim to Bryan in the SGG Matrix room every Monday through Friday at 10:00 America/Los_Angeles, using the existing scheduler timezone. Always send a status update, including when no qualifying fix release is available, so Bryan remembers why the PR is waiting and what comes next. Verify both npm packages and ancestry of upstream fix e0f67bdf3c5a0875dfa98b475648af37caac71a6 in the corresponding stable release tag. After the fix ships, continue reporting release availability on each scheduled workday until Bryan pauses or removes the reminder. No one-time notification marker or unchanged-result suppression. Source failures must be reported as failures, not as an unreleased fix. Do not install dependencies, edit the PR, request reviews, or publish anything. This reports availability for Bryan to update and validate the PR himself.
