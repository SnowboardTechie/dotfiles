Script-only, one-time TypeSpec stable-release watcher for SGG PR #1093. Deliver non-empty script stdout verbatim to Bryan in the SGG Matrix room; empty output means no qualifying release or notification already prepared. Daily 10:00 America/Los_Angeles, using the existing scheduler timezone. Verify both npm packages and ancestry of upstream fix e0f67bdf3c5a0875dfa98b475648af37caac71a6 in the corresponding stable release tag. Do not install dependencies, edit the PR, request reviews, or publish anything. This reports availability for Bryan to update and validate the PR himself.

The script writes a one-time notification-prepared marker under HERMES_HOME/state. That marker is not transport confirmation: if delivery fails, inspect the saved output and delivery error, then recover that message or clear the marker deliberately. Do not silently abandon a failed first delivery.
